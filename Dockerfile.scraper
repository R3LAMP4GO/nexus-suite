# Node 22 full (NOT alpine — Chromium needs glibc)
FROM node:22 AS scraper
WORKDIR /app
RUN corepack enable
COPY package.json package-lock.json ./
RUN npm ci --ignore-scripts
RUN npx patchright install chromium --with-deps
COPY . .
CMD ["node", "dist/server/worker/index.js"]
