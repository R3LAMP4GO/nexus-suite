Chunk 2 done. TypeScript passes clean.

**Changes:**

- **`prepare-context.ts`**: Added `CLIENT_PLUGIN_WHITELIST = ["organizationId", "input"]` — most restrictive whitelist, no `variables`/`config`/Infisical keys
- **`agent-delegate.ts`**:
  - Imported `CLIENT_PLUGIN_WHITELIST`
  - `resolveAgent` now returns `ResolvedAgent` with `isClientPlugin` flag
  - `executeAgentDelegate` builds a sandboxed context for client plugins, stripping everything except whitelist keys before passing to the plugin's generate function

Phase 2 done. Resume: `gh issue view 81`, execute chunk 3. Files: `src/agents/general/prepare-context.ts`, `src/server/workflows/agent-delegate.ts`. Issues: none.
