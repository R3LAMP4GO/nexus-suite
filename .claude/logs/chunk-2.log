Chunk 2 done. Created:

- **`src/server/services/r2-storage.ts`** — S3-compatible R2 client with `uploadFile`, `downloadFile`, `getSignedUrl`, `deleteFile`. Uses env vars from docker-compose.
- **`package.json`** — Added `@aws-sdk/client-s3` + `@aws-sdk/s3-request-presigner` deps.

Typecheck: all errors are missing `node_modules` (same as chunk 1 files — npm install blocked by unresolvable `next-auth@^5.0.0`). No code-level TS errors.

CLAUDE.md updated to chunk 2/3 done.
