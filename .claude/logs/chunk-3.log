Chunk 3/3 done. Summary of changes:

**New handlers:**
- `analytics-sync.ts` — resolves platform account, fetches credentials via Infisical fetch-use-discard, calls platform analytics API. Storage deferred until `AnalyticsMetric` model lands.
- `webhook-dispatch.ts` — POSTs payload with exponential backoff (3 retries, 1s base), 10s timeout per attempt.

**Wiring:**
- `jobs/index.ts` — registered both new handlers, kept `MEDIA_PROCESS` as lone stub.

**BullMQ cleanup:**
- `worker/index.ts` — removed `Worker` import, `bullWorkers` array, orphaned `"default"` queue worker, `REDIS_URL` check, and BullMQ close loop.

**Typecheck:** clean (`tsc --noEmit` passes).
queue worker, REDIS_URL check, BullMQ close loop

### Typecheck
- tsc --noEmit: PASS (fixed fetchSecret 4-arg signature)
