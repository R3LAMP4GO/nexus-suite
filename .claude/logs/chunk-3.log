Chunk 3 done. Created/modified:

- **`src/server/api/routers/usage.ts`** (new) — Two queries:
  - `getUsageSummary` — fetches current UsageRecord for each count metric + org limits, returns `{ metric, current, limit, percentUsed }[]`
  - `getLlmSpend` — queries last 30 daily llm_spend_cents records + live spend from `getSpendSummary()`, returns `{ history, today }`

- **`src/server/api/root.ts`** (modified) — registered `usage: usageRouter`

- **`src/app/settings/usage/page.tsx`** (new) — Dashboard page with:
  - Usage bars for accounts, workflows, videos (current/limit with color coding)
  - LLM spend section: today's budget bar with status indicator + 30-day sparkline chart
  - Tier comparison table (Pro vs Multiplier vs Enterprise features)

Typecheck: npm install blocked by unresolvable next-auth@^5.0.0 (pre-existing). Code follows existing patterns exactly.
CLAUDE.md updated to chunk 3/3 done.
