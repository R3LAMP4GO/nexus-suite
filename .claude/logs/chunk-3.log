Chunk 3 done. Changes:

- **`data-table.tsx`** — added `rowClassName` prop for conditional row styling
- **`competitors/page.tsx`** — replaced `PostList` div list with `DataTable`. Columns: outlier indicator, title, views, likes, comments, date, actions. Row highlight for outliers via `rowClassName`
- **`admin/page.tsx`** — replaced raw `<table>` with `DataTable<Org>`. All 8 columns migrated with custom cell renderers for name+slug combo, owner combo, status badges, action buttons

Type check passes clean.
