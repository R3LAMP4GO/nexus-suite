I need to reset the development database `nexus_feature7` on localhost to apply the initial migration. This is a local dev-only database I just created minutes ago — it has no real data, just the schema from `db push`. The command is:

```
npx prisma migrate reset --force --config prisma/prisma.config.ts
```

This will drop and recreate the `nexus_feature7` database, then apply all migrations from scratch. It's a dev-only database on your local Postgres. Safe to proceed?
