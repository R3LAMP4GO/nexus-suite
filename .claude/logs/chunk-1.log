Chunk 1 done. Created `src/server/services/platform-apis/linkedin.ts` with:

- **Mock mode**: returns `mock_linkedin_{uuid}` when `MOCK_PLATFORM_APIS=true`
- **3-step LinkedIn Marketing API flow**:
  1. Register upload → gets asset URN + upload URL
  2. PUT binary to upload URL with `application/octet-stream`
  3. POST `/v2/ugcPosts` with asset, caption as commentary, visibility PUBLIC
- **Signature**: `postLinkedInApi(accessToken, variation)` — matches YouTube/TikTok pattern
- Typecheck passes clean
